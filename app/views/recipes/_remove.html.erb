<%= form_for(current_user.reciperelationships.find_by(recipesaved_id: @recipe),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Remove", class: "btn btn-small btn-danger" %>
<% end %>
<% @body_class = "about" %>
<% provide(:title, 'All recipes') %>
<h1>All recipes</h1>
<div>
<%= render 'newest' %>
</div>

<%= will_paginate %>

<ul class="users">
	<%= render @recipes %>
</ul>

<%= will_paginate %>
